<h3>Editing Book</h3>

<%= form_for(@book) do |f| %>
  <h3>Title</h3>
  <%= f.text_field :title %>
  <p>Body</p>
  <%= f.text_area :body %>
  <br><%= f.submit 'Update Book' %></br>
<% end %>

<%= link_to "Show", book_path(@book) %>
 |
<%= link_to "Back", books_path(@book) %>